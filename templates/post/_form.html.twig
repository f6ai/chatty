{{ form_start(form) }}
{{ form_errors(form) }}

{{ form_row(form.title) }}

{{ form_row(form.text) }}

{{ form_row(form.topic) }}

<button type="submit" class="btn btn-primary">Submit</button>
{{ form_end(form) }}
